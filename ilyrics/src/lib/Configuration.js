const websiteLocalPort = 8888;
const musicUpdateTimeout = 1000; // null escape

const unsupportedRawPath = './src/dist/unsupported.json';
const unsupportedPath = '../dist/unsupported.json';

module.exports = {
    musicUpdateTimeout,
    websiteLocalPort,

    unsupportedRawPath,
    unsupportedPath
}